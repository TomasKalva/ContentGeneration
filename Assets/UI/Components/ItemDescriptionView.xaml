<UserControl x:Class="ContentGeneration.Assets.UI.Components.ItemDescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:util="clr-namespace:ContentGeneration.Assets.UI.Util"
             xmlns:model="clr-namespace:ContentGeneration.Assets.UI.Model"
             xmlns:comp="clr-namespace:ContentGeneration.Assets.UI.Components"
             mc:Ignorable="d"
             x:Name="parent"
             d:DataContext="{d:DesignInstance model:ItemState}">
    <StackPanel>
        <TextBlock Text="{Binding Name}" Background="Yellow" TextAlignment="Center">Name</TextBlock>
        <TextBlock Text="{Binding Description}" Background="Yellow" Height="100"  TextAlignment="Center">Description</TextBlock>
    </StackPanel>
</UserControl>
