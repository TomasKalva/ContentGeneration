<UserControl x:Class="OurFramework.UI.Components.ItemDescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:util="clr-namespace:OurFramework.UI.Util"
             xmlns:model="clr-namespace:OurFramework.UI.Model"
             xmlns:comp="clr-namespace:OurFramework.UI.Components"
             mc:Ignorable="d"
             x:Name="parent"
             d:DataContext="{d:DesignInstance model:ItemState}">
    <DockPanel Background="Black" VerticalAlignment="Stretch">
        <TextBlock Text="{Binding Name}" DockPanel.Dock="Top" Background="Beige" FontSize="22" TextAlignment="Center" Margin="0,0,0,3"></TextBlock>
        <TextBlock Text="{Binding Description}" DockPanel.Dock="Bottom" Background="Beige" TextAlignment="Center" TextWrapping="WrapWithOverflow"></TextBlock>
    </DockPanel>
</UserControl>
